import { createStore } from '@ice/store';

import panelStatus from '/Users/gary/git_thinkhub/thinkhub_client_web/src/pages/ThinkMind/models/panelStatus';

const models = { panelStatus };
const store = createStore(models);
export default store;
